<h1>Cats!</h1>

<input
  type="hidden"
  name="authenticity_token"
  value="<%= form_authenticity_token %>">
  
<ul>
  <% @cats.each do |cat| %>
    <li>
      <a href= "<%=cat_url(cat.id)%>"> <%= cat.name%> </a>
    </li>
  <% end %>
</ul>
